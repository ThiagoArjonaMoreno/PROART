def main():
    perguntas_por_grupo = {
        # ... (código das perguntas) ...
    }

    escolha = input("Deseja rodar a simulação automática? (s/n): ").strip().lower()
    if escolha == 's':
        while True:
            try:
                num_simulacoes = int(input("Quantas simulações você deseja realizar? "))
                if num_simulacoes > 0:
                    break
                else:
                    print("Por favor, insira um número maior que 0.")
            except ValueError:
                print("Entrada inválida. Digite um número inteiro.")

        dados_usuarios = simular_respostas(perguntas_por_grupo, num_simulacoes)
    else:
        dados_usuarios
